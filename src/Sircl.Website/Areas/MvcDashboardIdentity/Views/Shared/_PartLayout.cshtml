@{
    this.Context.Response.Headers.Add("X-Sircl-Document-Title", "MvcDashboardIdentity - " + ViewBag.Title);
}
<script>if (typeof appId === "undefined" || appId != "MvcDashboardIdentity") { location.reload(); }</script>
@RenderBody()