@{ 
    this.Context.Response.Headers.Add("X-Sircl-Document-Title", "MvcDashboardLocalize - " + ViewBag.Title);
}
<script>if (typeof appId === "undefined" || appId != "MvcDashboardLocalize") { location.reload(); }</script>
@RenderBody()