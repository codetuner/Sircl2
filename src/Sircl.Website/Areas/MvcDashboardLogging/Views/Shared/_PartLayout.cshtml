@{ 
    this.Context.Response.Headers.Add("X-Sircl-Document-Title", "MvcDashboardLogging - " + ViewBag.Title);
}
<script>if (typeof appId === "undefined" || appId != "MvcDashboardLogging") { location.reload(); }</script>
@RenderBody()